{
  "name": "build-plugin",
  "version": "1.15.0",
  "type": "module",
  "bin": {
    "buildctl": "source/skills/build/buildctl/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "check-sync": "node scripts/check-sync.js",
    "test": "node --test --test-concurrency=1 scripts/transformers/*.test.js"
  }
}
